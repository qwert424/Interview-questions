1、什么是布尔类型？
true|false

2、什么是布尔判定？
即运算结果为:'',0,null,undefined,NaN,false 均为否

3、什么是短路运算？
即 && || 两个运算符 结果是什么 取决于最后看的是哪一部分

```
0||'q' =>>'q'
1||'q' =>>1

callback&&callback() =>>callback()
0&&1 =>>0
&&也可以？代替
obj?a?b?c  
```

即 || 只要第一个为真 就返回第一个 否者返回第二个值
即 && 只要第一个为否 就返回第一个 否者返回第二个值


